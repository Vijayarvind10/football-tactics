[phases.setup]
nixPkgs = ["bash", "curl"]

[phases.install]
cmds = ["curl -fsSL https://bun.sh/install | bash", "export PATH=$HOME/.bun/bin:$PATH && bun install --frozen-lockfile --production"]

[start]
cmd = "export PATH=$HOME/.bun/bin:$PATH && bun run src/index.ts"
